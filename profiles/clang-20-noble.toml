# Clang 20 on Noble with workarounds for known toolchain issues.

[compiler]
type = "clang"
version = "20"

[target]
series = "noble"

[[flags]]
var = "DEB_CFLAGS_APPEND"
flag = "-gdwarf-4"
reason = "Noble's dwz 0.15 doesn't support DWARF5 (Clang 20 defaults to DWARF5)"

[[flags]]
var = "DEB_CXXFLAGS_APPEND"
flag = "-gdwarf-4"
reason = "Noble's dwz 0.15 doesn't support DWARF5 (Clang 20 defaults to DWARF5)"
